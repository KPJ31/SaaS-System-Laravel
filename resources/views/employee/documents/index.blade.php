@extends('layouts.app')
@section('title', 'My Documents - Elevanix')
@section('content')
@include('partials.page-header', ['eyebrow' => 'Employee', 'title' => 'My Documents', 'description' => 'Files connected to your assigned tasks and projects.'])
<section class="content-card mb-3"><form class="row g-2"><div class="col-md-3"><select class="form-select" name="project_id"><option value="">All projects</option>@foreach($projects as $project)<option value="{{ $project->id }}" @selected(request('project_id') == $project->id)>{{ $project->name }}</option>@endforeach</select></div><div class="col-md-3"><select class="form-select" name="task_id"><option value="">All tasks</option>@foreach($tasks as $task)<option value="{{ $task->id }}" @selected(request('task_id') == $task->id)>{{ $task->title }}</option>@endforeach</select></div><div class="col-md-2"><select class="form-select" name="type"><option value="">All types</option>@foreach(['pdf','docx','xlsx','png','jpg','zip'] as $type)<option value="{{ $type }}" @selected(request('type') === $type)>{{ strtoupper($type) }}</option>@endforeach</select></div><div class="col-md-2"><input class="form-control" type="date" name="date" value="{{ request('date') }}"></div><div class="col-md-2"><button class="btn btn-primary w-100"><i class="fa-solid fa-filter"></i>Filter</button></div></form></section>
<section class="content-card"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>File</th><th>Project</th><th>Task</th><th>Uploaded By</th><th>Date</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($files as $file)<tr><td><strong>{{ $file->original_name }}</strong><small>{{ number_format($file->size / 1024, 1) }} KB</small></td><td>{{ $file->project?->name ?? '-' }}</td><td>{{ $file->task?->title ?? '-' }}</td><td>{{ $file->uploader?->name ?? '-' }}</td><td>{{ $file->created_at->format('M d, Y') }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('employee.files.download', $file) }}"><i class="fa-solid fa-download"></i>Download</a></td></tr>@empty<tr><td colspan="6" class="empty-cell">@include('partials.empty-state', ['icon' => 'fa-folder-open', 'title' => 'No documents', 'message' => 'Accessible work files appear here.'])</td></tr>@endforelse</tbody></table></div>{{ $files->links() }}</section>
@endsection
