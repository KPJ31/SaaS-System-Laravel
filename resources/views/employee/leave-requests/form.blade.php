@extends('layouts.app')
@section('title', ($leave->exists ? 'Edit' : 'New').' Leave Request - Elevanix')
@section('content')
@include('partials.page-header', ['eyebrow' => 'Employee', 'title' => $leave->exists ? 'Edit Leave Request' : 'New Leave Request', 'description' => 'Pending requests can be edited or cancelled.'])
<section class="content-card"><form method="POST" action="{{ $leave->exists ? route('employee.leave-requests.update', $leave) : route('employee.leave-requests.store') }}" data-loading-form>@csrf @if($leave->exists) @method('PUT') @endif<div class="row g-3"><div class="col-md-4"><label class="form-label">Leave type <span class="required-mark">*</span></label><select class="form-select" name="leave_type" required>@foreach(['annual','casual','sick','unpaid','emergency','other'] as $type)<option value="{{ $type }}" @selected(old('leave_type', $leave->leave_type) === $type)>{{ ucfirst($type) }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Start date <span class="required-mark">*</span></label><input class="form-control" type="date" name="start_date" value="{{ old('start_date', $leave->start_date?->format('Y-m-d')) }}" required></div><div class="col-md-4"><label class="form-label">End date <span class="required-mark">*</span></label><input class="form-control" type="date" name="end_date" value="{{ old('end_date', $leave->end_date?->format('Y-m-d')) }}" required></div><div class="col-12"><label class="form-label">Reason <span class="required-mark">*</span></label><textarea class="form-control" name="reason" rows="5" required>{{ old('reason', $leave->reason) }}</textarea></div></div><div class="mt-3"><button class="btn btn-primary"><i class="fa-solid fa-floppy-disk"></i>Save Request</button></div></form></section>
@endsection
