@extends('layouts.app')
@section('title', 'Work Sessions - Elevanix')
@section('content')
@include('partials.page-header', ['eyebrow' => 'Employee', 'title' => 'My Work Sessions', 'description' => 'Review your daily, weekly and monthly tracked time.', 'actions' => new \Illuminate\Support\HtmlString('<a class="btn btn-outline-primary" href="'.route('employee.work-sessions.export', request()->query()).'"><i class="fa-solid fa-download"></i>Export CSV</a><a class="btn btn-primary" href="'.route('employee.work-sessions.pdf', request()->query()).'"><i class="fa-solid fa-file-pdf"></i>Export PDF</a>')])
<div class="stat-grid">@include('partials.stat-card', ['label' => 'Today Minutes', 'value' => $dailyTotal, 'icon' => 'fa-clock']) @include('partials.stat-card', ['label' => 'Week Minutes', 'value' => $weeklyTotal, 'icon' => 'fa-calendar-week', 'tone' => 'green']) @include('partials.stat-card', ['label' => 'Month Minutes', 'value' => $monthlyTotal, 'icon' => 'fa-calendar-days', 'tone' => 'blue'])</div>
<section class="content-card mb-3"><form class="row g-2"><div class="col-md-3"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search notes"></div><div class="col-md-3"><select class="form-select" name="project_id"><option value="">All projects</option>@foreach($projects as $project)<option value="{{ $project->id }}" @selected(request('project_id') == $project->id)>{{ $project->name }}</option>@endforeach</select></div><div class="col-md-3"><select class="form-select" name="task_id"><option value="">All tasks</option>@foreach($tasks as $task)<option value="{{ $task->id }}" @selected(request('task_id') == $task->id)>{{ $task->title }}</option>@endforeach</select></div><div class="col-md-2"><input class="form-control" type="date" name="date" value="{{ request('date') }}"></div><div class="col-md-1"><button class="btn btn-primary w-100"><i class="fa-solid fa-filter"></i></button></div></form></section>
<section class="content-card"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Date</th><th>Project</th><th>Task</th><th>Start</th><th>Stop</th><th>Duration</th><th>Note</th><th>Status</th></tr></thead><tbody>@forelse($sessions as $session)<tr><td>{{ $session->started_at?->format('M d, Y') }}</td><td>{{ $session->project?->name ?? '-' }}</td><td>{{ $session->task?->title ?? '-' }}</td><td>{{ $session->started_at?->format('H:i') }}</td><td>{{ $session->ended_at?->format('H:i') ?? 'Running' }}</td><td>{{ $session->duration_minutes }} min</td><td>{{ $session->notes ?? '-' }}</td><td>@include('partials.status-badge', ['status' => $session->status ?? ($session->ended_at ? 'stopped' : 'running')])</td></tr>@empty<tr><td colspan="8" class="empty-cell">@include('partials.empty-state', ['icon' => 'fa-clock', 'title' => 'No work sessions', 'message' => 'Tracked time appears here.'])</td></tr>@endforelse</tbody></table></div>{{ $sessions->links() }}</section>
@endsection
