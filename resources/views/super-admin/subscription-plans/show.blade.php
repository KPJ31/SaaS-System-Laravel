@extends('layouts.app')

@section('title', 'Subscription Plan - Elevanix')

@section('content')
@include('partials.page-header', ['eyebrow' => 'Subscription Plans', 'title' => $plan->name, 'description' => 'View pricing, limits, features and company usage.', 'actions' => new Illuminate\Support\HtmlString('<a class="btn btn-outline-primary" href="'.route('super-admin.subscription-plans.edit', $plan).'"><i class="fa-regular fa-pen-to-square"></i> Edit</a>')])
<div class="row g-3"><div class="col-lg-5"><section class="content-card"><h2>Plan Details</h2><dl class="detail-list mt-3"><dt>Code</dt><dd>{{ $plan->slug }}</dd><dt>Monthly</dt><dd>${{ number_format($plan->monthly_price, 2) }}</dd><dt>Yearly</dt><dd>${{ number_format($plan->annual_price ?? 0, 2) }}</dd><dt>Employees</dt><dd>{{ $plan->employee_limit }}</dd><dt>Projects</dt><dd>{{ $plan->project_limit }}</dd><dt>Storage</dt><dd>{{ $plan->storage_limit_mb }} MB</dd><dt>Status</dt><dd>@include('partials.status-badge', ['status' => $plan->status])</dd></dl></section></div><div class="col-lg-7"><section class="content-card"><h2>Features</h2><ul class="check-list mt-3">@forelse($plan->features ?? [] as $feature)<li><i class="fa-solid fa-check"></i>{{ $feature }}</li>@empty<li>No features listed.</li>@endforelse</ul></section></div><div class="col-12"><section class="content-card"><h2>Companies Using This Plan</h2><div class="table-responsive mt-3"><table class="table"><thead><tr><th>Company</th><th>Status</th><th>Renews</th></tr></thead><tbody>@forelse($plan->subscriptions as $subscription)<tr><td>{{ $subscription->company?->name }}</td><td>@include('partials.status-badge', ['status' => $subscription->status])</td><td>{{ $subscription->renews_at?->format('M d, Y') ?? '-' }}</td></tr>@empty<tr><td colspan="3" class="empty-cell">No companies are using this plan yet.</td></tr>@endforelse</tbody></table></div></section></div></div>
@endsection
